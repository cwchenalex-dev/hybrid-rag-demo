services:
  rag-api:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "127.0.0.1:8000:8000"

    env_file:
      - .env

    volumes:
      - ./.kb:/app/.kb

    restart: unless-stopped